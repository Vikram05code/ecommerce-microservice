spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/productDB
    username: ${DB_USER}
    password: ${DB_PASSWORD}

    #url: jdbc:postgresql://ep-flat-band-a1w1udex-pooler.ap-southeast-1.aws.neon.tech/productDB?sslmode=require&channel_binding=require
    #username: neondb_owner
    #password: npg_nzLkBHQSVX20
  jpa:
    database: postgresql
    show-sql: true
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  rabbitmq:
    hots: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include:"*"
